package Polymorphism;
/**
 * NOTE -method signature == method name + argument list
 * Rule 1. there must be a inheritance relationship between Child and Parent
 * Rule 2. Method name must be same 
 * Rule 3. Argument list must be same
 * Rule 4. Return type might be same or covarient Return type
 * Rule 5. Access modifier might be same or must be higher scope access modifier
 * Rule 6. With respect to exception will discuss in respective  class
 * Rule 7. We can not override private methods(not visible in other classes)
 * Rule 8. We can not override static method
 * Rule 9. We can not override final method
 */
public class OverRide {

}
