package javainterview;

public class TSACylinder {
	public static void main(String[] args) {
		double r = 3, h = 5;
		double area = 2 * Math.PI * r * (h + r);
		System.out.println("Total Surface Area = " + area);
	}
}